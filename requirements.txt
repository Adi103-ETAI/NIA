# Core dependencies
pyttsx3
keyboard
vosk
sounddevice
soundfile
pygame

# Testing
pytest
pytest-asyncio

# TTS dependencies (optional: Coqui TTS requires MSVC build tools)
# Coqui TTS: pip install TTS
torch
torchaudio
numpy

# STT dependencies
SpeechRecognition
silero_vad
# Optional STT enhancements
deepfilternet  # DeepFilterNet for noise suppression

# LangChain dependencies
langchain
langchain_core
langchain_ollama
lancedb
farm-haystack

# Configuration
PyYAML

